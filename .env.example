MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=
JWT_SECRET=
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# URL da API embutida no build do front (porta publicada pelo servico api)
VITE_API_URL=http://localhost:3000
# Origem do front liberada no CORS da API (porta publicada pelo servico client)
CORS_ORIGIN=http://localhost:8080
